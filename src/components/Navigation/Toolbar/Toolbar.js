import React from 'react';

import "./Toolbar.scss";

const toolbar = (props) => {
    return (
        <header className="Toolbar">
            <div>MENU</div>
            <div>LOGO</div>
            <nav>
                ...
            </nav>
        </header>
    );
}

export default toolbar;
